{
  "name": "pdf_ocr",
  "description": "A simple workflow that extracts images from a PDF, preprocesses them, and then runs OCR.",
  "execution_mode": "simple",
  "pipeline": [
    {
      "id": "extract_images",
      "name": "pdf_extraction_processor",
      "params": {
        "resolution": 300,
        "image_format": "PNG"
      }
    },
    {
      "id": "preprocess_image",
      "name": "image_preprocessing_processor",
      "dependencies": ["extract_images"],
      "params": {
        "steps": [
          {
            "name": "deskew"
          },
          {
            "name": "denoise"
          }
        ]
      }
    },
    {
      "id": "run_ocr",
      "name": "ocr_processor",
      "dependencies": ["preprocess_image"],
      "params": {
        "language": "eng"
      }
    }
  ]
}