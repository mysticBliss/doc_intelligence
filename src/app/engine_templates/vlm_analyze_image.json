{
  "name": "vlm_analyze_image",
  "description": "An advanced pipeline for when simple text extraction is not enough. It sends an image to a Vision Language Model (VLM) for complex analysis, such as describing the image content or answering questions about it.",
  "execution_mode": "simple",
  "pipeline": [
    {
      "id": "analyze_with_vlm",
      "name": "vlm_processor",
      "params": {
        "prompt": "Get Key data in JSON format from this image.",
        "model": "qwen2.5vl:3b",
        "temperature": 0.2,
        "max_tokens": 1024
      }
    }
  ]
}